version: '3.8'
services:
  flask-app:
    build: .
    image: ecomdiv-ai-autoassign  # Custom image name
    ports:
      - "8080:8080"
    volumes:
      - ./app/data:/app/data
    environment:
      - PORT=8080
